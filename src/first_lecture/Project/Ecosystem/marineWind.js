class MarineWind {
    constructor(){
        this.tx = random(0, 1000)
        this.ty = random(0, 1000)
        this.force = this.createForce()
    }

    createForce() {
        let force = createVector()

        return force
    }

    update(){
        
    }
}